# Copy to backend/.env for local overrides. Defaults already match docker-compose.
# DATABASE_URL=postgresql+asyncpg://docvault:docvault@localhost:5432/docvault
# LOG_LEVEL=INFO
# LOG_JSON=false
# CORS_ORIGINS=["http://localhost:3000"]
